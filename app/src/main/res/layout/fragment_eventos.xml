<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/eventosFragmentRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".View.ui.fragments.EventosFragment">

    <!-- RecyclerView horizontal para eventos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvEventos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:paddingVertical="16dp"
        android:clipToPadding="false"
        tools:listitem="@layout/item_evento"
        android:overScrollMode="never" />

    <!-- ProgressBar centrado en la pantalla -->
    <ProgressBar
        android:id="@+id/progressBarEventos"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:visibility="gone" />

</FrameLayout>
